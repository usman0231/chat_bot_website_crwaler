{
  "name": "sitebot-widget",
  "version": "0.1.0",
  "private": true,
  "description": "Embeddable chat widget for SiteBot — single-file JS, no deps at runtime.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
